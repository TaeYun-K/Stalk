<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.Stalk.project.api.ai.dao.AnalysisResultMapper">

  <insert id="insert" parameterType="com.Stalk.project.api.ai.entity.AnalysisResult" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO analysis_result (
      video_recording_id, original_file_name, gcs_uri, analysis_summary, created_at
    ) VALUES (
               #{videoRecordingId}, #{originalFileName}, #{gcsUri}, #{analysisSummary}, #{createdAt}
             )
  </insert>


  <select id="findByVideoRecordingId" resultType="com.Stalk.project.api.ai.entity.AnalysisResult">
    SELECT
      id,
      video_recording_id,
      original_file_name,
      gcs_uri,
      analysis_summary,
      created_at
    FROM
      analysis_result
    WHERE
      video_recording_id = #{videoRecordingId}
    ORDER BY created_at DESC
    LIMIT 1
  </select>

</mapper>

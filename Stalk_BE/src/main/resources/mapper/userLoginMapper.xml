<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.Stalk.project.api.login.dao.UserLoginMapper">

  <select id="findByActiveUserId" resultType="com.Stalk.project.api.signup.entity.User">
    SELECT *
    FROM users
    WHERE user_id = #{userId}
      AND is_active = 1
  </select>

  <update id="update">
    UPDATE users
    SET last_login_at = #{lastLoginAt},
        updated_at = CURRENT_TIMESTAMP
    WHERE id = #{id}
  </update>

</mapper>